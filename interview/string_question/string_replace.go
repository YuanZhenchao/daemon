package string_question

/**
将hasytack 中的 oldString 替换成 newString
实现函数 strings.Replace("oink oink oink", "k", "ky", 2)
 */
func StringReplace(hasytack,oldString,newString string,count int) string {
	res := ""
	if "" == hasytack || "" == oldString {
		return ""
	}
	//"" == newString

	for  {
		
	}

	return res
}
